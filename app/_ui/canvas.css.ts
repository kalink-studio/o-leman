import { sys } from "@kalink-ui/seedly/styles";
import { style } from "@vanilla-extract/css";

export const canvasWrapper = style({
  padding: sys.spacing[6],
  border: `1px solid ${sys.color.foreground}`,
});

export const canvasStyle = style({});
